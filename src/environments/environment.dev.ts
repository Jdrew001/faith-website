// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    BASE_URL: 'http://antsle.ddns.me/faith-service/',
    ASSET_URL: '/faith-website/assets/',
    PAY_PAL_URL: 'https://api.paypal.com/',
    REDIRECT_PAGE: 'http://discoverfaitharlington.org/give/',
    SITE_KEY: '6Ldr2UwaAAAAADNVANgDHqV6E4437tWi7VILBQq1',
    CMS_URL: 'http://192.168.1.212:1337'
  };
  
  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/dist/zone-error';  // Included with Angular CLI.
  