import { LazyloaderDirective } from './lazyloader.directive';

describe('LazyloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyloaderDirective();
    expect(directive).toBeTruthy();
  });
});
